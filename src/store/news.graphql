query News($category: String!) {
  news(category: $category) {
    title
    url
    metadata {
      image
      description
    }
    newsworthy {
      ... on TvShow {
        id
        name
      }
    }
  }
}
